# Port for backend server
PORT=4000

# PostgreSQL connection string
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/fieldservicely

# Google Maps server key (used for server-side requests if needed)
GOOGLE_MAPS_API_KEY=YOUR_GOOGLE_MAPS_SERVER_KEY